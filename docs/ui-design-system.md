# UI design system and reusable components

The frontend foundation defines design tokens for color, spacing, radius, and typography plus accessible reusable components. Clinical screens should use these components consistently to support high-density workflows, clear alert severity, and future theme governance.
